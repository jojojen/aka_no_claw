# Virtual environments
.venv/
venv/

# Local secrets
.env
.env.*
!.env.example
config/llm_pool.json

# Python caches
__pycache__/
*.py[cod]
*.pyd
.pytest_cache/
.mypy_cache/

# Build / packaging
*.egg-info/
build/
dist/

# Local databases and runtime data
data/*.sqlite
data/*.sqlite3
data/*.sqlite3-shm
data/*.sqlite3-wal
data/*.sqlite3-journal
data/*.sqlite3.bak*
data/*.db
data/*.db-shm
data/*.db-wal
data/*.log
data/quiz_song_packs/
# contains notify_chat_id (personal data) — never commit
data/vpn_rotation.json
!data/.gitkeep
logs/
run/

# Dynamically self-written tools (/new) + their dedicated venv
generated_tools/
.openclaw_tmp/
.openclaw_ocr/
reports/

# Regenerable spike embedding caches (compare.py rebuilds on run)
spikes/**/emb_*.npz

# Runtime-generated comp-filter IDF corpus (TitleCorpusRebuilder rebuilds weekly)
data/market_title_df.json

# Runtime workflow data (live execution state; fixtures go under tests/fixtures/)
workflow_store/

# OS / editor noise
.DS_Store
Thumbs.db
